{
  "name": "game-of-life",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "wasm-rust": "cd wasm && wasm-pack build --target no-modules --no-typescript --release && cp pkg/wasm_bg.wasm pkg/wasm.js ../_site/js/",
    "release": "npm run wasm-rust && shadow-cljs release app",
    "watch": "shadow-cljs watch app"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "shadow-cljs": "^2.16.8"
  },
  "dependencies": {
    "uhtml": "^2.8.0"
  }
}
